import 'package:flutter/material.dart';


class ListVictimTemplate extends StatelessWidget {
  const ListVictimTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return const Placeholder();
  }
}
